
const spacings = {
    spacing_s: '2px',
    spacing_m: '5px',
    spacing_l: '10px',
    spacing_xl: '15px',
}

const widths = {
    width_max: '100%'
};

const heights ={
    height_max: '100vh',
};

const layoutSetting = {
    ...spacings,
    ...widths,
    ...heights,
};

export default layoutSetting;